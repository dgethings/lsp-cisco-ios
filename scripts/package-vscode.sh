#!/bin/bash

# This is a placeholder script for packaging the VSCode extension.
# In a real-world scenario, this would use a tool like `vsce` to package the extension.

echo "Packaging VSCode extension..."
mkdir -p dist
touch dist/cisco-ios-lsp-vscode.vsix
echo "VSCode extension packaged successfully."
