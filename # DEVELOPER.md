# Developer Notes

To enable debugging set the last param to `true` in `server.go`. Logs are written to `/tmp/lsp/.log`

To apply to the LSP to the file run the following vim commands from the `lsp` dir.

```vim
source nvim.lua
setfiletype IOS-JINJA
```
